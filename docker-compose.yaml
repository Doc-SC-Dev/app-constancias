services:
  next-app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        DATABASE_URL: ${DATABASE_URL}
        RESEND_API_KEY: ${RESEND_API_KEY}
    ports:
      - 3000:3000
    env_file:
      - .env
    stdin_open: true
    tty: true
    restart: always